name: CI
on: push
jobs:
    build:
        runs-on: ${{ matrix.runner }}
        strategy:
            fail-fast: false
            matrix:
                include:
                    - runner: ubuntu-24.04
                    - runner: ubuntu-24.04-arm

        steps:
            - uses: actions/checkout@v2

            - name: build
              run: make
